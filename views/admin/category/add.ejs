<title><%= pageTitle %></title>
<%- include('../partials/header') %>
<%- include('../partials/navbar') %>
<%- include('../partials/sidebar') %>
<div class="content-wrapper">
  <section class="content-header">
        <div class="container-fluid">
          <div class="row mb-2">
            <div class="col-sm-6">
              <h1>Catgroy</h1>
            </div>
            <div class="col-sm-6">
              <ol class="breadcrumb float-sm-right">
                <li class="breadcrumb-item"><a href="/admin/dashboard">Home</a></li>
                <li class="breadcrumb-item active">Category</li>
              </ol>
            </div>
          </div>
        </div><!-- /.container-fluid -->
      </section>
      <section class="content">
        <div class="container-fluid">
          <div class="row">
            <div class="col-md-12">
              <div class="card">
                <div class="card-header">
                  <h3 class="card-title">Category Add</h3>
                </div>
                <div class="card-body">
                  <form action="/admin/category/add" method="POST">
                    <div class="form-group">
                      <label for="">Category</label>
                      <input type="text" name="categoryName" placeholder="Enter Category" class="form-control">
                      <% if(errors && errors.categoryName){ %>
                        <span class="text-danger"><%= errors.categoryName.msg %></span>
                        <% } %>
                    </div>
                    <div class="form-group">
                      <label for="">Category Description</label>
                     <textarea name="description" id="" class="form-control"></textarea>
                     <% if(errors && errors.description){ %>
                      <span class="text-danger"><%= errors.description.msg %></span>
                      <% } %>
                    </div>
                    <div class="form-group">
                      <input type="submit" class="btn btn-primary bt-sm" value="Add Category">
                    </div>

                  </form>
                </div>
              </div>
            </div>
          </div>
        </div>
      </section>
     
    <!-- /.content -->
  </div>
  

<%- include('../partials/script') %>
<%- include('../partials/footer') %>